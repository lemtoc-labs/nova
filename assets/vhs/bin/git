#!/usr/bin/env bash

set -euo pipefail

if [[ -z "${NOVA_VHS_REAL_GIT:-}" ]]; then
  echo "NOVA_VHS_REAL_GIT is not set" >&2
  exit 127
fi

sleep "${NOVA_VHS_GIT_DELAY:-1}"
exec "$NOVA_VHS_REAL_GIT" "$@"
