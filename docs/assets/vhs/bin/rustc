#!/usr/bin/env bash

set -euo pipefail

if [[ -z "${NOVA_VHS_REAL_RUSTC:-}" ]]; then
  echo "NOVA_VHS_REAL_RUSTC is not set" >&2
  exit 127
fi

sleep "${NOVA_VHS_RUSTC_DELAY:-2}"
exec "$NOVA_VHS_REAL_RUSTC" "$@"
